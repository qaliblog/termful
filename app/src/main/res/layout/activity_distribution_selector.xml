<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/termux_background_color">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Choose Linux Distribution"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/termux_primary_text_color"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Select a Linux distribution to download and install, or skip to use the minimal bootstrap."
        android:textSize="14sp"
        android:textColor="@color/termux_secondary_text_color"
        android:layout_marginBottom="24dp" />

    <ListView
        android:id="@+id/distribution_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/termux_background_color"
        android:divider="@color/termux_divider_color"
        android:dividerHeight="1dp"
        android:choiceMode="singleChoice" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="16dp">

        <Button
            android:id="@+id/skip_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Skip (Minimal)"
            android:layout_marginEnd="8dp"
            android:background="@color/termux_button_color"
            android:textColor="@color/termux_button_text_color" />

        <Button
            android:id="@+id/download_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Download"
            android:layout_marginStart="8dp"
            android:background="@color/termux_button_color"
            android:textColor="@color/termux_button_text_color"
            android:enabled="false" />

    </LinearLayout>

</LinearLayout>